@model IEnumerable<da.Models.KHUVUC>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tên_khu_vực)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tên_khu_vực)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Mã_khu_vực }) |
            @Html.ActionLink("Details", "Details", new { id=item.Mã_khu_vực }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Mã_khu_vực })
        </td>
    </tr>
}

</table>
