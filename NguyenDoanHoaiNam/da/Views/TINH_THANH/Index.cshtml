@model IEnumerable<da.Models.TINH_THANH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tên_tỉnh_thành)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KHUVUC.Tên_khu_vực)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tên_tỉnh_thành)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KHUVUC.Tên_khu_vực)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Mã_tỉnh_thành }) |
            @Html.ActionLink("Details", "Details", new { id=item.Mã_tỉnh_thành }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Mã_tỉnh_thành })
        </td>
    </tr>
}

</table>
